# AGENTS - `template_storybook/docs`

Keep docs aligned with the public fork contract: data in `content/`, rendering
logic in `src/storybook/`, thin scripts in `scripts/`, and generated artifacts
under `output/`.
