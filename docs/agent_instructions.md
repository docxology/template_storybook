# Agent Instructions

1. Read `AGENTS.md`.
2. Change story data in `content/story.yaml`.
3. Change rendering behavior in `src/storybook/`.
4. Keep scripts as thin selectors.
5. Verify with tests and Stage-02 PDF generation.
