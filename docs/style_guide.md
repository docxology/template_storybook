# Style Guide

The bundled art uses symbolic geometry, large calm shapes, high contrast text,
and page-specific palettes. Direct text overlays should use bright text with a
shadow. Boxed overlays should use translucent warm paper.
