# AGENTS - `template_storybook/content`

`story.yaml` is the source of truth for story text, character records, page
order, scene selection, palettes, and text overlay mode. Change content here,
then rerender through `scripts/`.
