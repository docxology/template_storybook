# AGENTS - `template_storybook/src`

Business logic for the storybook exemplar lives here. Scripts may choose a page
or artifact, but they must not own character construction, image composition,
text overlay, or PDF assembly.

Keep the public API in `storybook/__init__.py` aligned with tests and docs.
