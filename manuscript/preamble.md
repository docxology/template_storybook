# Preamble

This manuscript describes the `template_storybook` exemplar. The primary
artifact is the generated storybook PDF, while this manuscript documents the
template mechanics for the shared repository renderer.
